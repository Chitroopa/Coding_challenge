class Solution:
    def sortedSquares(self, A: List[int]) -> List[int]:
        result = []
        
        for i in range(len(A)):
            result.append(A[i]**2)
            
        result.sort()
        return result
        
